query userPrivateProfile($id: String!) {
  user(id: $id) {
    id
    email
    profile {
      displayName
      locale
      language

      title
      firstName
      middleName
      lastName
      suffix
    }
  }
}
