#import "./TestModule.graphql"

mutation updateTestModule(
  $data: TestModuleUpdateInput!
  $where: TestModuleWhereUniqueInput!
) {
  updateTestModule(data: $data, where: $where) {
    node {
      ...TestModuleInfo
    }
    errors {
      field
      message
    }
  }
}
