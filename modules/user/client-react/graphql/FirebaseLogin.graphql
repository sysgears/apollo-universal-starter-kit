#import "./User.graphql"
mutation firebaseLogin($input: FirebaseLoginUserInput!) {
  firebaseLogin(input: $input) {
    tokens {
      accessToken
      refreshToken
    }
    user {
      ...UserInfo
    }
    errors {
      field
      message
    }
  }
}
