#import "./$Module$.graphql"

mutation edit$Module$($input: Edit$Module$Input!) {
  edit$Module$(input: $input) {
    node {
      ...$Module$Info
    }
    errors {
      field
      message
    }
  }
}
