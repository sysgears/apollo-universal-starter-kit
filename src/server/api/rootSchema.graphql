type FieldError {
  field: String!
  message: String!
}

type PageInfo {
  totalCount: Int
  hasNextPage: Boolean
}

type Query {
  dummy: Int
}

type Mutation {
  dummy: Int
}

type Subscription {
  dummy: Int
}

schema {
  query: Query
  mutation: Mutation
  subscription: Subscription
}
