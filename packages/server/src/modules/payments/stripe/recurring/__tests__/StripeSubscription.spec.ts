import { getApollo } from '../../../../../testHelpers/integrationSetup';

describe('Stripe subscription API works', () => {
  let apollo: string;

  before(() => {
    apollo = getApollo();
  });
});
