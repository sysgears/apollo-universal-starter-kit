{
  "name": "apollo-fullstack-starter-kit",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "seed": "lerna run seed",
    "watch": "lerna run watch --stream"
  },
  "devDependencies": {
    "lerna": "^2.0.0-rc.5"
  }
}
