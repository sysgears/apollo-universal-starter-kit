subscription onPostUpdated($endCursor: ID!) {
    postsUpdated(endCursor: $endCursor) {
        mutation
        id
        endCursor
        node {
            id
            title
            content
        }
    }
}