#import "./Product.graphql"

mutation createProduct($data: ProductCreateInput!) {
  createProduct(data: $data) {
    node {
      ...ProductInfo
    }
    errors {
      field
      message
    }
  }
}
