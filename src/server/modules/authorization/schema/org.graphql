extend type Query {
}

extend type Mutation {
}

extend type Subscription {
}

type OrgRoleInfo {
  orgId: ID!
  orgName: String
  scopes: [String]
  roles: [RoleInfo]
}

extend type Org {
  roles: [RoleInfo]
}

extend type User {
  orgRoles: [OrgRoleInfo]
}
