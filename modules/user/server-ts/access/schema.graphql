type LogoutFromAllDevicesPayload {
  userId: Int!
  mutation: String!
}

extend type Subscription {
  # Subscription for logout from all devices
  logoutFromAllDevicesSub(userId: Int!): LogoutFromAllDevicesPayload!
}
