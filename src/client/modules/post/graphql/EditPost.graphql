#import "./Post.graphql"

mutation editPost($input: EditPostInput!) {
    editPost(input: $input) {
        user {
            ... UserInfo
        }
        errors {
            field
            message
        }
    }
}
