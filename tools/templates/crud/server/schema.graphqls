input Add$Module$Input {
  name: String!
}

input Edit$Module$Input {
  id: Int!
  name: String!
}

extend type Query {
  $module$s: [$Module$]
  $module$(id: Int!): $Module$
}

extend type Mutation {
  add$Module$(input: Add$Module$Input!): $Module$
  edit$Module$(input: Edit$Module$Input!): $Module$
  delete$Module$(id: Int!): $Module$
}

extend type Subscription {}