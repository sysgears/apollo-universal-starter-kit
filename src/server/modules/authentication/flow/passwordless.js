/*eslint-disable no-unused-vars*/

// send email with JWT token link
export const passwordlessLogin = inputUser => {};

// handle the user's clicking and callback with a JWT token link thingy
export const passwordlessCallback = (req, res) => {};
