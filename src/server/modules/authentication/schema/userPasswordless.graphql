extend type Query {
}

extend type Mutation {
  # Login user (no need to register, just create first time seen)
  loginPasswordless(input: PasswordlessLoginInput!): UserPayload!
}

extend type Subscription {
}

input PasswordlessLoginInput {
  email: String!
}
