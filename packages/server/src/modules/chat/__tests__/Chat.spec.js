/*eslint-disable no-unused-vars*/
import { getApollo } from '../../../testHelpers/integrationSetup';

describe('Chat API works', () => {
  let apollo;

  before(() => {
    apollo = getApollo();
  });
});
