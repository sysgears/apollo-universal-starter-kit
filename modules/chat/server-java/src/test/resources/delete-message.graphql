mutation deleteMessage($id: Int!) {
  deleteMessage(id: $id) {
    id
    text
    userId
    createdAt
    username
    uuid
    quotedId
    filename
    path
    quotedMessage {
      id
      text
      username
      filename
      path
    }
  }
}
