fragment UserInfo on User {
    id
    username
    isAdmin
    isActive
    email
    profile {
        firstName
        lastName
        fullName
    }
    auth {
        certificate {
            serial
        }
        facebook {
            fbId
            displayName
        }
    }
}
