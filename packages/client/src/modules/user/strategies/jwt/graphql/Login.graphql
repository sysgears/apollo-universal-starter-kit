#import "../../../graphql/User.graphql"
mutation login($input: LoginUserInput!) {
    login(input: $input) {
        tokens {
            token
            refreshToken
        }
        user {
            ...UserInfo
        }
        errors {
            field
            message
        }
    }
}
