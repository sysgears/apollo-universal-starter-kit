#import "./$Module$.graphql"

mutation add$Module$($input: Add$Module$Input!) {
  add$Module$(input: $input) {
    node {
      ...$Module$Info
    }
    errors {
      field
      message
    }
  }
}
