#import "./Group.graphql"

query groups($orderBy: OrderByInput, $filter: FilterInput) {
  myGroups(orderBys: [$orderBy], filters: [$filter]) {
    groups {
      ...GroupInfo
    }
    errors {
      field
      message
    }
  }
}
