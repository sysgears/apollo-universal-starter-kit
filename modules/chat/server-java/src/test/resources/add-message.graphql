mutation addMessage($input: AddMessageInput!) {
  addMessage(input: $input) {
    id
    text
    userId
    createdAt
    username
    uuid
    quotedId
    filename
    path
    quotedMessage {
      id
      text
      username
      filename
      path
    }
  }
}
