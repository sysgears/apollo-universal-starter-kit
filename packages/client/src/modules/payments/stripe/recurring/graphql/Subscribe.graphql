#import "./Subscription.graphql"

mutation addStripeSubscription($input: StripeSubscribtionInput!) {
  addStripeSubscription(input: $input) {
    ...StripeSubscription
    errors {
      field
      message
    }
  }
}
