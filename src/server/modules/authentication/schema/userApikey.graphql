extend type Mutation {
  createUserApiKey(id: String!, name: String!): ApiKeyPayload!
  deleteUserApiKey(id: String!, name: String!): ApiKeyPayload!
}

extend type UserAuth {
  apikeys: [ApiKeyAuth]
}
