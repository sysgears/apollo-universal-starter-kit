#import "../../../common/graphql/User.graphql"
mutation login($input: LoginUserInput!) {
    login(input: $input) {
        user {
            ...UserInfo
        }
        errors {
            field
            message
        }
    }
}
