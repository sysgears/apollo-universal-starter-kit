#import "./comment.graphql"

subscription onUpdateComment($postId: Int!) {
    updateComment(postId: $postId) {
        mutation
        id
        postId
        node {
            ... CommentInfo
        }
    }
}
