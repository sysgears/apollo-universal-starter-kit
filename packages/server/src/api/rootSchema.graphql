type FieldError {
  field: String!
  message: String!
}

type PageInfo {
  totalCount: Int
  hasNextPage: Boolean
}

type BatchPayload {
  count: Int
  errors: [FieldError!]
}

type Query {
  dummy: Int
}

type Mutation {
  dummy: Int
}

type Subscription {
  dummy: Int
}

schema {
  query: Query
  mutation: Mutation
  subscription: Subscription
}
