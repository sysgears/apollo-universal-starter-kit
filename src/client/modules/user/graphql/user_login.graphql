mutation login($input: LoginUserInput!) {
    login(input: $input) {
        tokens {
            token,
            refreshToken
        }
        errors {
            field
            message
        }
    }
}
