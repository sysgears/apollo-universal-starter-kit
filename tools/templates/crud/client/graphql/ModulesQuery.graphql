#import "./$Module$.graphql"

query $module$s($limit: Int, $offset: Int, $orderBy: OrderByInput, $filter: $Module$FilterInput) {
  $module$s(limit: $limit, offset: $offset, orderBy: $orderBy, filter: $filter) {
    pageInfo {
      totalCount
      hasNextPage
    }
    edges {
      ...$Module$Info
    }
  }
}
