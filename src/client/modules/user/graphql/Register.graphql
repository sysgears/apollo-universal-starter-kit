#import "./User.graphql"

mutation register($input: RegisterUserInput!) {
  register(input: $input) {
    user {
      ...UserInfo
    }
    tokens {
      token
      refreshToken
    }
    errors {
      field
      message
    }
  }
}
