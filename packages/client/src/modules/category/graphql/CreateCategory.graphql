#import "./Category.graphql"

mutation createCategory($data: CategoryCreateInput!) {
  createCategory(data: $data) {
    node {
      ...CategoryInfo
    }
    errors {
      field
      message
    }
  }
}
