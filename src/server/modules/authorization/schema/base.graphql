extend type Query {
}

extend type Mutation {
}

extend type Subscription {
}

type RoleInfo {
  id: ID
  name: String
  roleId: ID
  roleName: String
  displayName: String
  description: String
  scopes: [String]
}
