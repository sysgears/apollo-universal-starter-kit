import DomainSchema from '@domain-schema/core';
import { SubCategory } from '../product/schema';

//eslint-disable-next-line import/prefer-default-export
export const SubCategorySchema = new DomainSchema(SubCategory);
