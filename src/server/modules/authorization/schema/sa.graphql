extend type Query {
}

extend type Mutation {
}

extend type Subscription {
}

type ServiceAccountRoleInfo {
  id: ID!
  name: String
  scopes: [String]
}

extend type ServiceAccount {
  serviceaccountRoles: [UserRoleInfo]
}
