import DomainSchema from '@domain-schema/core';
import { Category } from '../product/schema';

//eslint-disable-next-line import/prefer-default-export
export const CategorySchema = new DomainSchema(Category);
